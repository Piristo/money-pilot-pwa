# Инструкция по загрузке на GitHub

## Быстрый способ (Командная строка)

Откройте терминал в папке `money-pilot` и выполните следующие команды:

```bash
# 1. Инициализируем Git репозиторий (если ещё не сделано)
git init

# 2. Добавляем удалённый репозиторий
git remote add origin https://github.com/Piristo/money-pilot-pwa.git

# 3. Добавляем все файлы
git add .

# 4. Создаём коммит
git commit -m "Initial commit: MoneyPilot PWA v1.0.0"

# 5. Отправляем на GitHub
git push -u origin main
```

### Если возникнет ошибка "branch main doesn't exist"

Выполните:
```bash
git branch -M main
git push -u origin main
```

### Если попросит логин/пароль

GitHub больше не принимает пароли. Используйте Personal Access Token:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите срок действия и права доступа (минимум: `repo`)
4. Скопируйте токен
5. Используйте токен вместо пароля при push

---

## Альтернатива: GitHub Desktop (Проще)

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Установите и войдите в аккаунт
3. File → Add Local Repository → выберите папку `money-pilot`
4. Если попросит создать репозиторий - согласитесь
5. Напишите описание коммита: "Initial commit: MoneyPilot PWA v1.0.0"
6. Нажмите "Commit to main"
7. Repository → Push origin

---

## После успешной загрузки на GitHub

### Деплой на Vercel

1. Перейдите: https://vercel.com/
2. Войдите через GitHub аккаунт
3. Нажмите "Add New..." → "Project"
4. Выберите репозиторий `money-pilot-pwa`
5. Vercel автоматически определит Next.js
6. Нажмите "Deploy"

**Настройки (автоматические):**
- Framework Preset: Next.js
- Build Command: `npm run build`
- Output Directory: `.next`
- Install Command: `npm install`

### Готово! 🎉

Ваше приложение будет доступно по адресу:
`https://money-pilot-pwa.vercel.app` (или похожему)

---

## Проверка перед деплоем

✅ Production build успешен (`npm run build`)
✅ `.gitignore` настроен правильно
✅ `node_modules` не будут загружены
✅ Все TypeScript ошибки исправлены
✅ Документация готова (README.md, DEPLOYMENT_GUIDE.md)

---

## Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть удалённые репозитории
git remote -v

# Посмотреть историю коммитов
git log --oneline

# Обновить код после изменений
git add .
git commit -m "Описание изменений"
git push
```

---

## Нужна помощь?

- GitHub Docs: https://docs.github.com/
- Vercel Docs: https://vercel.com/docs
- Next.js Deployment: https://nextjs.org/docs/deployment
