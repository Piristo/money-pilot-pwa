# План улучшения Авто модуля + Supabase 🚗

## Дата: 10.02.2026

---

## Часть 1: Улучшение Авто модуля на 100%

### 1. Метрики и аналитика авто

**Новые поля в store:**
```typescript
// Характеристики авто
carBrand: string;           // Марка (например: Toyota)
carModel: string;           // Модель (например: Camry)
carYear: number;            // Год выпуска
carMileage: number;         // Текущий пробег (км)
fuelConsumption: number;    // Расход л/100км
fuelType: string;           // Тип топлива (Бензин АИ-95, Дизель)
tankCapacity: number;       // Объём бака (л)

// Цены
fuelPrice: number;          // Текущая цена за литр
```

**Расчётные метрики:**
- Стоимость за км (₽/км)
- Средний расход топлива в месяц (л)
- Прогноз расходов на месяц (₽)
- Стоимость полного бака (₽)
- Запас хода на полном баке (км)
- Средняя стоимость заправки

### 2. Категории расходов авто

**Типы расходов:**
- ⛽ Топливо (бензин, дизель, газ)
- 🔧 ТО и ремонт (плановое обслуживание, ремонт)
- 🚗 Мойка и уход (мойка, химчистка, полировка)
- 🅿️ Парковка (платная парковка, абонемент)
- 📋 Страховка (ОСАГО, КАСКО)
- 🛣️ Штрафы и налоги (штрафы ГИБДД, транспортный налог)
- 🔩 Запчасти (покупка запчастей)
- 🛞 Шины (покупка, шиномонтаж, хранение)

**Визуализация:**
- Круговая диаграмма расходов по категориям
- Линейный график расходов по месяцам
- Топ-3 самых дорогих категорий

### 3. Журнал заправок

**Функционал:**
- Запись каждой заправки (дата, литры, сумма, пробег)
- Расчёт реального расхода топлива
- История цен на топливо
- Статистика: средний чек, частота заправок
- График динамики расхода

**Поля записи:**
```typescript
interface FuelLog {
  id: number;
  date: string;
  liters: number;        // Залито литров
  amount: number;        // Сумма
  mileage: number;       // Пробег на момент заправки
  pricePerLiter: number; // Цена за литр
  fuelType: string;      // Тип топлива
  station: string;       // АЗС (опционально)
  fullTank: boolean;     // Полный бак?
}
```

### 4. Журнал ТО и ремонтов

**Функционал:**
- История всех работ
- Напоминания о плановом ТО (по пробегу или дате)
- Категории работ (масло, фильтры, тормоза, подвеска)
- Прикрепление чеков (фото)
- Статистика по типам работ

**Поля записи:**
```typescript
interface MaintenanceLog {
  id: number;
  date: string;
  type: 'maintenance' | 'repair';
  category: string;      // Категория работ
  description: string;   // Описание
  amount: number;        // Стоимость
  mileage: number;       // Пробег
  workshop: string;      // СТО
  nextServiceMileage?: number; // След. ТО через N км
  nextServiceDate?: string;    // След. ТО через N дней
}
```

### 5. Умные напоминания

**Типы напоминаний:**
- По пробегу (ТО через 5000 км)
- По дате (ОСАГО через 30 дней)
- По сезону (Смена резины в ноябре)
- Повторяющиеся (Мойка каждые 2 недели)

**Уведомления:**
- Push-уведомления (если PWA)
- Badge на иконке авто в навигации
- Список просроченных напоминаний

### 6. Дашборд авто

**Карточки метрик:**
- Расходы за месяц (₽)
- Средний расход топлива (л/100км)
- Стоимость за км (₽/км)
- Пробег за месяц (км)
- Дней до следующего ТО
- Дней до окончания ОСАГО

**Графики:**
- Динамика расходов (по месяцам)
- Распределение по категориям (pie chart)
- Расход топлива (тренд)

### 7. Экспорт и отчёты

**Функционал:**
- Экспорт в CSV (все расходы)
- PDF отчёт за период
- Статистика за год
- Сравнение месяцев

---

## Часть 2: Интеграция с Supabase

### Зачем Supabase?

**Преимущества:**
- ✅ Синхронизация между устройствами
- ✅ Резервное копирование данных
- ✅ Мультиплатформенность (web, mobile)
- ✅ Совместный доступ (семейный бюджет)
- ✅ Real-time обновления
- ✅ Аутентификация пользователей
- ✅ Хранение файлов (чеки, фото)

### Архитектура

**Гибридный подход (offline-first):**
1. Локальное хранилище (localStorage) - основное
2. Supabase - синхронизация и бэкап
3. Автоматическая синхронизация при наличии интернета
4. Работа без интернета (offline mode)

### Структура базы данных

**Таблицы:**

```sql
-- Пользователи (через Supabase Auth)
users (
  id uuid primary key,
  email text,
  created_at timestamp
)

-- Профили
profiles (
  id uuid primary key references users(id),
  name text,
  currency text,
  locale text,
  car_name text,
  car_brand text,
  car_model text,
  car_year int,
  car_mileage int,
  fuel_consumption decimal,
  fuel_type text,
  tank_capacity decimal,
  fuel_price decimal
)

-- Транзакции
transactions (
  id uuid primary key,
  user_id uuid references users(id),
  title text,
  amount decimal,
  type text, -- income/expense
  date timestamp,
  category_id text,
  subcategory_id text,
  budget_id uuid,
  created_at timestamp,
  updated_at timestamp
)

-- Бюджеты
budgets (
  id uuid primary key,
  user_id uuid references users(id),
  name text,
  limit_amount decimal,
  used_amount decimal,
  created_at timestamp
)

-- Авто расходы
auto_expenses (
  id uuid primary key,
  user_id uuid references users(id),
  title text,
  amount decimal,
  category text, -- fuel, maintenance, parking, etc.
  date timestamp,
  created_at timestamp
)

-- Журнал заправок
fuel_logs (
  id uuid primary key,
  user_id uuid references users(id),
  date timestamp,
  liters decimal,
  amount decimal,
  mileage int,
  price_per_liter decimal,
  fuel_type text,
  station text,
  full_tank boolean
)

-- Журнал ТО
maintenance_logs (
  id uuid primary key,
  user_id uuid references users(id),
  date timestamp,
  type text, -- maintenance/repair
  category text,
  description text,
  amount decimal,
  mileage int,
  workshop text,
  next_service_mileage int,
  next_service_date timestamp
)

-- Напоминания
reminders (
  id uuid primary key,
  user_id uuid references users(id),
  title text,
  due_date timestamp,
  type text, -- mileage/date/recurring
  completed boolean,
  created_at timestamp
)

-- Подписки
subscriptions (
  id uuid primary key,
  user_id uuid references users(id),
  name text,
  price decimal,
  color text,
  billing_date int, -- день месяца
  created_at timestamp
)
```

### Этапы интеграции

**Этап 1: Настройка Supabase (30 мин)**
1. Создать проект на supabase.com
2. Установить `@supabase/supabase-js`
3. Настроить env переменные
4. Создать клиент Supabase

**Этап 2: Аутентификация (1 час)**
1. Страница входа/регистрации
2. Email + Password auth
3. Google OAuth (опционально)
4. Сохранение сессии
5. Защита роутов

**Этап 3: Миграция схемы (1 час)**
1. Создать таблицы в Supabase
2. Настроить Row Level Security (RLS)
3. Создать индексы
4. Настроить триггеры

**Этап 4: Синхронизация (2-3 часа)**
1. Создать sync service
2. Загрузка данных при входе
3. Автосохранение при изменениях
4. Обработка конфликтов
5. Offline queue

**Этап 5: Real-time (1 час)**
1. Подписка на изменения
2. Обновление UI в реальном времени
3. Уведомления о синхронизации

**Этап 6: Storage для файлов (1 час)**
1. Загрузка чеков/фото
2. Оптимизация изображений
3. Отображение в UI

---

## Приоритеты реализации

### Фаза 1: Улучшение авто модуля (4-5 часов)
1. ✅ Метрики и аналитика (1 час)
2. ✅ Категории расходов (30 мин)
3. ✅ Журнал заправок (1.5 часа)
4. ✅ Журнал ТО (1 час)
5. ✅ Дашборд с графиками (1 час)

### Фаза 2: Supabase интеграция (6-7 часов)
1. ✅ Настройка проекта (30 мин)
2. ✅ Аутентификация (1 час)
3. ✅ Миграция схемы (1 час)
4. ✅ Синхронизация (2-3 часа)
5. ✅ Real-time (1 час)
6. ✅ Storage (1 час)

**Общее время: 10-12 часов**

---

## Технологии

**Frontend:**
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion
- Recharts

**Backend:**
- Supabase (PostgreSQL)
- Supabase Auth
- Supabase Storage
- Supabase Realtime

**Библиотеки:**
```bash
npm install @supabase/supabase-js
npm install @supabase/auth-helpers-nextjs
npm install date-fns # для работы с датами
```

---

## Структура файлов

```
src/
├── lib/
│   ├── supabase/
│   │   ├── client.ts           # Supabase клиент
│   │   ├── auth.ts             # Аутентификация
│   │   ├── sync.ts             # Синхронизация
│   │   └── storage.ts          # Работа с файлами
│   ├── auto/
│   │   ├── metrics.ts          # Расчёт метрик
│   │   ├── fuel-logs.ts        # Журнал заправок
│   │   └── maintenance.ts      # Журнал ТО
│   └── app-store.tsx           # Обновлённый store
├── app/
│   ├── auth/
│   │   ├── login/page.tsx      # Вход
│   │   └── register/page.tsx   # Регистрация
│   └── auto/
│       ├── page.tsx            # Дашборд
│       ├── fuel/page.tsx       # Журнал заправок
│       ├── maintenance/page.tsx # Журнал ТО
│       └── settings/page.tsx   # Настройки авто
└── components/
    ├── auto/
    │   ├── MetricsCard.tsx     # Карточка метрики
    │   ├── FuelChart.tsx       # График расхода
    │   └── CategoryChart.tsx   # График категорий
    └── auth/
        ├── LoginForm.tsx       # Форма входа
        └── RegisterForm.tsx    # Форма регистрации
```

---

## Следующие шаги

**Что делаем сначала?**

1. **Улучшаем авто модуль** (без Supabase)
   - Добавляем метрики
   - Журнал заправок
   - Журнал ТО
   - Графики

2. **Потом добавляем Supabase**
   - Настройка проекта
   - Аутентификация
   - Синхронизация

**Или делаем всё сразу?**

Рекомендую начать с улучшения авто модуля, а потом добавить Supabase. Так будет проще тестировать и отлаживать.

---

## Готов начать! 🚀

С чего начнём?

1. **Метрики и аналитика** - добавим расчёты стоимости за км, расхода топлива
2. **Журнал заправок** - полноценный трекинг заправок с расчётом расхода
3. **Supabase setup** - сразу настроим бэкенд

**Ваш выбор?** 😊
